sudo apt update -y && sudo apt upgrade -y
sudo apt-get install unzip lsof ffmpeg build-essential -y
sudo apt-get update -y

wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh
rm Miniconda3-latest-Linux-x86_64.sh

ssh-keygen -t ed25519 -C "dhrvmohapatra@gmail.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub

----->>> add key and close

git clone git@github.com:dhxmo/autonosis-server-mvp-01.git
conda create -n core python=3.10
conda activate core

export HF_TOKEN=___

pip3 install git+https://github.com/mlfoundations/open_clip.git@main
pip install ray "pip install "ray[serve]" python-multipart

pip install gdown
mkdir model && gdown 1VKTSgxy67T063Yf3EB-BuFR50kB8726Y

serve build main:caption_app -o config.yaml
serve run config.yaml